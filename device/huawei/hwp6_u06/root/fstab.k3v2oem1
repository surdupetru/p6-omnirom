# Android fstab file.
#<src>                                                  <mnt_point>         <type>    <mnt_flags and options>                       <fs_mgr_flags>
# The filesystem that contains the filesystem checker binary (typically /system) cannot
# specify MF_CHECK, and must come before any filesystems that do specify MF_CHECK
## DTS2013082709807 w00177577 fstab.k3v2oem1 
/devices/platform/hi_mci.0/mmc_host/mmc1                       auto    vfat      defaults                                      voldmanaged=sdcard1:auto,noemulatedsd
/devices/hisik3-usb-otg/usb1                       /mnt/usb    vfat      defaults                                      voldmanaged=usbotg:auto
